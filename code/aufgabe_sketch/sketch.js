const sketchWidth = 400;
const sketchHeight = 400;

function preload(){
  // preload assets
}


function setup() {
  createCanvas(sketchWidth, sketchHeight);
}

function draw() {
  background(150);
  
  
  // for(let x = 0; x <= sketchWidth; x += 10) {
  // for(let y = 0; y <= sketchHeight; y += 10) {

  //    strokeWeight(1);  
  //    line(0, 0, 100, 100);
    
  //   strokeWeight(10);
  //    line(100, 100, 200, 200);
    
  //    strokeWeight(20);
  //    line(200, 200, 400, 400);

  }
}  

}
